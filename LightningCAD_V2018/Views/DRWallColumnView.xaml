<local:ViewBase x:Class="LightningCAD.Views.DRWallColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightningCAD.Views"
             Title="墙柱结构识别">
    <Border BorderThickness="3" Background="LightGray">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"></ColumnDefinition>
                <ColumnDefinition Width="3"></ColumnDefinition>
                <ColumnDefinition Width="120"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="Red" BorderThickness="1">
                <Grid Background="LightGray">
                    <Label HorizontalContentAlignment="Center" VerticalAlignment="Top" Background="Transparent" Content="平面◈">
                        <Label.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="清空" Click="MenuItem1_Click"/>
                                <MenuItem Header="删除无效" Click="MenuItem2_Click"/>
                            </ContextMenu>
                        </Label.ContextMenu>
                    </Label>
                    <DataGrid CanUserAddRows="False" Height="500" Margin="0,22,0,0" VerticalAlignment="Top" AutoGenerateColumns="False" Name="planeDG" ItemsSource="{Binding}" IsReadOnly="True" MouseDoubleClick="PlaneDG_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="编号" Width="55" Binding="{Binding Name, Mode=TwoWay}"></DataGridTextColumn>
                            <DataGridCheckBoxColumn Header="有效" Width="35" Binding="{Binding IsValid, Mode=TwoWay}"></DataGridCheckBoxColumn>
                            <DataGridCheckBoxColumn Header="不同" Width="35" Binding="{Binding IsDifferent, Mode=TwoWay}"></DataGridCheckBoxColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid Margin="0,523,0,0">
                        <Button Content="识别" HorizontalAlignment="Left" Margin="1,0,0,0" VerticalAlignment="Top" Width="35" Height="20" Click="PlaneRead_Click"/>
                        <Button Content="修改" HorizontalAlignment="Left" Margin="42,0,0,0" VerticalAlignment="Top" Width="35" Height="20" Click="PlaneModify_Click"/>
                        <Button Content="删除" HorizontalAlignment="Left" Margin="83,0,0,0" VerticalAlignment="Top" Width="35" Height="20" Click="PlaneDelete_Click"/>
                        <Button Content="比较" HorizontalAlignment="Right" Margin="0,0,1,0" VerticalAlignment="Top" Width="35" Height="20" Click="Compare_Click"/>
                        <Button Name="filterBtn" Content="全部" HorizontalAlignment="Left" Margin="1,21,0,1" VerticalAlignment="Top" Width="35" Height="20" Click="Filter_Click"/>
                        <Button Name="categoryBtn" Content="墙柱" HorizontalAlignment="Left" Margin="42,21,0,1" VerticalAlignment="Top" Width="35" Height="20" Click="Category_Click"/>
                        <Button Content="导出Revit" HorizontalAlignment="Right" Margin="0,21,1,1" VerticalAlignment="Top" Width="74" Height="20" Click="Export_Click"/>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="371"></RowDefinition>
                    <RowDefinition Height="3"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Border BorderBrush="Red" BorderThickness="1" Grid.Row="0">
                    <Grid Background="LightGray">
                        <Label HorizontalContentAlignment="Center" VerticalAlignment="Top" Background="Transparent">柱大样</Label>
                        <DataGrid Name="detailDG" Height="304" Margin="0,22,0,0" VerticalAlignment="Top" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding}" AutoGenerateColumns="False" MouseDoubleClick="DetailDG_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="编号" Binding="{Binding Name, Mode=TwoWay}" Width="55"></DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Grid Margin="0,327,0,0">
                            <TextBlock Margin="3,2.5,0,0">大样放大倍数：</TextBlock>
                            <TextBox Margin="0,0,1,0" VerticalAlignment="Top" Width="35" Height="20" Name="multiple" Text="4" HorizontalAlignment="Right" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                            <Button Margin="1,21,0,1" HorizontalAlignment="Left" VerticalAlignment="Top" Width="35" Height="20" Content="识别" Click="DetailRead_Click"/>
                            <Button Margin="0,21,0,1" HorizontalAlignment="Center" VerticalAlignment="Top" Width="35" Height="20" Content="修改" Click="DetailModify_Click"/>
                            <Button Margin="0,21,1,1" HorizontalAlignment="Right" VerticalAlignment="Top" Width="35" Height="20" Content="删除" Click="DetailDelete_Click"/>
                        </Grid>
                    </Grid>
                </Border>

                <Border BorderBrush="Red" BorderThickness="1" Grid.Row="2">
                    <Grid Background="LightGray">
                        <Label HorizontalContentAlignment="Center" VerticalAlignment="Top" Background="Transparent">墙表</Label>
                        <DataGrid Name="wallInfoDG" Height="147" Margin="0,22,0,0" VerticalAlignment="Top" ItemsSource="{Binding}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="False" FrozenColumnCount="1" CanUserReorderColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="编号" Binding="{Binding Name, Mode=TwoWay}" Width="45"></DataGridTextColumn>
                                <DataGridTextColumn Header="厚度" Binding="{Binding Thickness, Mode=TwoWay}" Width="60"></DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Grid Margin="0,170,0,0" FlowDirection="RightToLeft">
                            <Button Content="删除" HorizontalAlignment="Left" Margin="1,0,0,1" VerticalAlignment="Top" Width="35" Height="20" Click="WallInfoDelete_Click"/>
                            <Button Content="新增" HorizontalAlignment="Center" Margin="0,0,0,1" VerticalAlignment="Top" Width="35" Height="20" Click="WallInfoAdd_Click"/>
                            <Button Content="识别" HorizontalAlignment="Right" Margin="0,0,1,1" VerticalAlignment="Top" Width="35" Height="20" Click="WallInfoRead_Click"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</local:ViewBase>
