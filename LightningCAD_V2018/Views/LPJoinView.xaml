<local:ViewBase x:Class="LightningCAD.Views.LPJoinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightningCAD.Views"
             Title="PDF合并">
    <Grid Height="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,3,0,0">
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="AddButton_Click">添加</Button>
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="DeleteButton_Click">删除</Button>
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="ClearButton_Click">清空</Button>
            <Button Width="25" Height="22" Margin="3,0,0,0" Click="MoveNumberButton_Click" Name="uper">
                <Image Name="up"></Image>
            </Button>
            <Button Width="25" Height="22" Margin="3,0,0,0" Click="MoveNumberButton_Click">
                <Image Name="down"></Image>
            </Button>
            <Button Width="40" Height="22" Margin="12,0,0,0" Click="OKButton_Click">保存</Button>
        </WrapPanel>

        <DataGrid Grid.Row="1" Name="pdfList" Margin="3,3,3,3" VerticalAlignment="Top" AutoGenerateColumns="False" ItemsSource="{Binding}" MaxHeight="500" MaxWidth="800" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="False" FrozenColumnCount="1" CanUserReorderColumns="False">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Resources>
                <Style x:Key="CenterTextBlockStyle" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
                <Style x:Key="LeftTextBlockStyle" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" IsReadOnly="True" ElementStyle="{StaticResource CenterTextBlockStyle}" Binding="{Binding Number, Mode=OneWay}"></DataGridTextColumn>
                <DataGridTextColumn Header="文件" IsReadOnly="True" ElementStyle="{StaticResource LeftTextBlockStyle}" Binding="{Binding FileName, Mode=OneWay}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:ViewBase>
