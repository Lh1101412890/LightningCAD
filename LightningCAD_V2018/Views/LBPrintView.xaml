<local:ViewBase x:Class="LightningCAD.Views.LBPrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:core="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:LightningCAD.Views"
             Title="批量打印" MinWidth="280" MinHeight="140">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0">
            <Label Height="25" FontSize="12" VerticalContentAlignment="Center" VerticalAlignment="Center" Margin="0,3,0,0">文件名:</Label>
            <TextBox Name="fileName" Width="375" Height="24" TextWrapping="NoWrap" Text="Lightning批量打印" VerticalContentAlignment="Center" FontSize="11" Margin="0,3,3,0" VerticalAlignment="Center"/>
        </WrapPanel>

        <WrapPanel Grid.Row="1">
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="AddRangeButton_Click">框选</Button>
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="AutoRangeButton_Click">自动</Button>
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="ResetRangeButton_Click">范围</Button>
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="DeleteRangeButton_Click">删除</Button>
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="ClearRangeButton_Click">清空</Button>
            <Button Width="25" Height="22" Margin="3,0,0,0" Click="MoveNumberButton_Click" Name="uper">
                <Image Name="up"></Image>
            </Button>
            <Button Width="25" Height="22" Margin="3,0,0,0" Click="MoveNumberButton_Click">
                <Image Name="down"></Image>
            </Button>
            <ComboBox SelectedIndex="0" VerticalContentAlignment="Center" Margin="3,0,0,0" Name="single" Height="22">
                <Label>单文件</Label>
                <Label>多文件</Label>
            </ComboBox>
            <Button Width="40" Height="22" Margin="3,0,0,0" Click="PrintButton_Click">打印</Button>
            <Button Width="40" Height="22" Margin="3,0,3,0" Click="MoreButton_Click">更多</Button>
        </WrapPanel>
        <WrapPanel Grid.Row="2">
            <Button Width="83" Height="22" Margin="3,0,0,0" Click="BoundingBox_Click" Content="纵向范围框" Name="verticalBox"></Button>
            <Button Width="83" Height="22" Margin="3,0,0,0" Click="BoundingBox_Click" Content="横向范围框"></Button>
            <RadioButton VerticalAlignment="Center" Margin="3,0,0,0" IsChecked="True" Checked="RadioButton_Checked">全部</RadioButton>
            <RadioButton VerticalAlignment="Center" Margin="3,0,0,0" Name="custom" Checked="Custom_Checked">自定义</RadioButton>
            <TextBox Name="range" IsEnabled="False" Height="22" Width="145" VerticalContentAlignment="Center" VerticalAlignment="Center" Margin="3,0,0,0"/>
        </WrapPanel>

        <DataGrid Grid.Row="3" Name="dataGrid" Margin="3,0,3,3" VerticalAlignment="Top" AutoGenerateColumns="False" ItemsSource="{Binding}" MaxHeight="500" MaxWidth="421" MouseDoubleClick="DataGrid_MouseDoubleClick" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="False" FrozenColumnCount="1" CanUserReorderColumns="False">
            <DataGrid.ContextMenu>
                <ContextMenu MenuItem.Click="ContextMenu_Click">
                    <MenuItem Header="尺寸">
                        <Label>A4</Label>
                        <Label>A3</Label>
                        <Label>A2</Label>
                        <Label>A1</Label>
                        <Label>A0</Label>
                    </MenuItem>
                    <MenuItem Header="方向">
                        <Label>纵向</Label>
                        <Label>横向</Label>
                    </MenuItem>
                    <MenuItem Header="线宽">
                        <Label>true</Label>
                        <Label>false</Label>
                    </MenuItem>
                    <MenuItem Header="透明度">
                        <Label>true</Label>
                        <Label>false</Label>
                    </MenuItem>
                    <MenuItem Header="样式">
                        <Label>黑白</Label>
                        <Label>彩色</Label>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Resources>
                <Style x:Key="CenterTextBlockStyle" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
                <Style x:Key="LeftTextBlockStyle" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" IsReadOnly="True" ElementStyle="{StaticResource CenterTextBlockStyle}" Binding="{Binding Number, Mode=OneWay}"></DataGridTextColumn>
                <DataGridTextColumn Header="尺寸" IsReadOnly="True" ElementStyle="{StaticResource CenterTextBlockStyle}" Binding="{Binding Size, Mode=OneWay}"></DataGridTextColumn>
                <DataGridTextColumn Header="方向" IsReadOnly="True" ElementStyle="{StaticResource CenterTextBlockStyle}" Binding="{Binding Direction, Mode=OneWay}"></DataGridTextColumn>
                <DataGridTextColumn Header="线宽" IsReadOnly="True" ElementStyle="{StaticResource CenterTextBlockStyle}" Binding="{Binding LineWeight, Mode=OneWay}"></DataGridTextColumn>
                <DataGridTextColumn Header="透明度" IsReadOnly="True" ElementStyle="{StaticResource CenterTextBlockStyle}" Binding="{Binding Transparent, Mode=OneWay}"></DataGridTextColumn>
                <DataGridTextColumn Header="样式" IsReadOnly="True" ElementStyle="{StaticResource CenterTextBlockStyle}" Binding="{Binding Style, Mode=OneWay}"></DataGridTextColumn>
                <DataGridTextColumn Header="备注" IsReadOnly="False" ElementStyle="{StaticResource LeftTextBlockStyle}" Binding="{Binding Note, Mode=TwoWay}" Width="197" x:Name="noteName"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:ViewBase>