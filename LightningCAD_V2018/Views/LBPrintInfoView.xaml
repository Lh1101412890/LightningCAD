<local:ViewBase x:Class="LightningCAD.Views.LBPrintInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LightningCAD.Views"
             mc:Ignorable="d" 
             Title="打印记录">
    <Grid MinWidth="260">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" Margin="0,2,0,0">
            <Button Height="22" Name="copy" x:FieldModifier="public" Margin="3,0,0,0">复制范围</Button>
            <Button Height="22" Name="check" x:FieldModifier="public" Margin="3,0,0,0">清理失效记录</Button>
            <Button Height="22" Name="delete" x:FieldModifier="public" Margin="3,0,0,0">删除记录</Button>
            <Button Height="22" Name="clear" x:FieldModifier="public" Margin="3,0,0,0">清空记录</Button>
            <Label FontSize="10" Name="fileSize" x:FieldModifier="public" VerticalContentAlignment="Center" HorizontalContentAlignment="Left"></Label>
        </WrapPanel>
        <DataGrid ItemsSource="{Binding}" Name="dataGrid" x:FieldModifier="public" AutoGenerateColumns="False" Grid.Row="1" MaxHeight="500" IsReadOnly="True" Margin="0,2,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文件路径" Binding="{Binding}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:ViewBase>
